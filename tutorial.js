//piece of code that does one or moreactions

var name = prompt();


function(first, second) {
    return first+second;
}

var sum = add (1, 2)
}
//name is the first argument and gets passed

//dry = do not repeat yourself

